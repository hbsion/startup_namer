import 'package:flutter/material.dart';

class SportPage extends StatelessWidget {
  final String title;

  SportPage({Key key, this.title}) : super(key: key);

  @override
  Widget build(BuildContext context) {
   
    return new Center(child: new Text('Sport Page!'));
  }
}